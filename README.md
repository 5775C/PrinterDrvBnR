## Usage
Just run _Bakcup.cmd for bakcup, run _Restore.cmd for Restore

Folder name prefix [X] means ping test failed printer  
  ┗ _Resotre.cmd won't install driver if folder name include '[X]'
Folder name prefix [√] means default printer

This script only can backup printer which port name is Ip address.
